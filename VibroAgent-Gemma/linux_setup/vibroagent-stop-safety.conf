[Service]
# The unit is Type=oneshot: only the launcher/control process is signalled.
# USB logger children must not be force-killed on ExecStop failure or timeout.
KillMode=mixed
SendSIGKILL=no
TimeoutStopSec=300
